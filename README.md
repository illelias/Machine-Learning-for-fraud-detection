# Machine-Learning-for-fraud-detection
This notebook applies machine learning models to detect fraudulent transactions in a bank dataset, using preprocessing, feature selection, and evaluation metrics like accuracy and ROC-AUC to assess model performance.
